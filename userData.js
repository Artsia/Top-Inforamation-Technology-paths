//when user enters a string and pushes enter button(event)
//we want to do something(action) ie get the text entered and store it
//on a sql relational db

//we want to create a method/ function that does this action when user
//enters string into form, where id = userName
const button = document.querySelector('btn');

function getTypedData()
{
    return Math.floor(Math.random() * (number+1));
}

btn.onclick = function() {
    const rndCol = 'rgb(' + random(255) + ',' + random(255) + ',' + random(255) + ')';
    document.body.style.backgroundColor = rndCol;
}